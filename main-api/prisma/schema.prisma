generator client {
  provider = "prisma-client"
  output   = "../src/db/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

enum UserRole {
  MASTER_ADMIN

  @@map("user_role")
}

enum SensitiveDocumentType {
  CPF
  CNPJ

  @@map("sensitive_document_type")
}

enum CommercialEntityType {
  INDIVIDUAL
  LEGAL_ENTITY

  @@map("commercial_entity_type")
}

enum EmploymentState {
  ACTIVE
  TERMINATED

  @@map("employment_state")
}

enum MachineType {
  YELLOW_LINE
  WHITE_LINE

  @@map("machine_type")
}

enum MachineIdentifierKind {
  PLATE
  COMPANY_TAG

  @@map("machine_identifier_kind")
}

enum MachineMeterType {
  HOUR_METER
  ODOMETER

  @@map("machine_meter_type")
}

enum MachineMeterReadingPurpose {
  INITIAL
  OWNERSHIP_TRANSFER
  ORDINARY

  @@map("machine_meter_reading_purpose")
}

enum MachineMeterReadingStatus {
  CONFIRMED

  @@map("machine_meter_reading_status")
}

enum ProjectLifecycleStatus {
  PLANNED
  ACTIVE
  PAUSED
  COMPLETED
  CANCELLED

  @@map("project_lifecycle_status")
}

enum ProjectWorkFrontStatus {
  PLANNED
  ACTIVE
  PAUSED
  COMPLETED
  CANCELLED

  @@map("project_work_front_status")
}

enum ProjectWorkFrontEmployeeAssignmentSource {
  DIRECT
  MACHINE_OPERATOR
  BOTH

  @@map("project_work_front_employee_assignment_source")
}

enum ProjectDailyReportShift {
  DAY
  NIGHT

  @@map("project_daily_report_shift")
}

enum ProjectDailyReportStatus {
  DRAFT
  FINALIZED

  @@map("project_daily_report_status")
}

enum ProjectDailyReportActivityType {
  EARTHWORKS
  DRAINAGE
  PAVING

  @@map("project_daily_report_activity_type")
}

enum ProjectDailyReportClimateCondition {
  RAIN
  DRY
  WATERLOGGED_SOIL

  @@map("project_daily_report_climate_condition")
}

enum ProjectProductionStatus {
  DRAFT
  APPROVED

  @@map("project_production_status")
}

enum ProjectProductionEntryMode {
  DIRECT_TOTAL
  TRIPS

  @@map("project_production_entry_mode")
}

enum ProjectProductionVolumeCondition {
  CUT
  LOOSE
  COMPACTED

  @@map("project_production_volume_condition")
}

enum ProjectProductionProfile {
  GENERIC
  EXCAVATION
  LOADING
  TRANSPORT
  SPREADING
  GRADING
  COMPACTION

  @@map("project_production_profile")
}

enum ProjectProductionDmtPolicy {
  NOT_APPLICABLE
  OPTIONAL
  REQUIRED

  @@map("project_production_dmt_policy")
}

enum ProjectProductionEquipmentRole {
  EXCAVATION
  LOADING
  TRANSPORT
  SPREADING
  GRADING
  COMPACTION
  WATERING
  SUPPORT

  @@map("project_production_equipment_role")
}

enum ProjectProductionRevisionEvent {
  CREATED
  UPDATED
  APPROVED
  DIRECT_APPROVED
  REOPENED
  TRIP_ADDED
  TRIP_REMOVED
  RDO_CONFIRMED

  @@map("project_production_revision_event")
}

model Corporation {
  id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name      String   @db.VarChar(160)
  isActive  Boolean  @default(true) @map("is_active")
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  domains                        Domain[]
  companies                      Company[]
  users                          User[]
  sessions                       Session[]
  sensitiveDocumentHarnessRows   SensitiveDocumentProtectionHarness[]
  clients                        Client[]
  fuelSuppliers                  FuelSupplier[]
  persons                        Person[]
  employments                    Employment[]
  jobRoles                       JobRole[]
  machines                       Machine[]
  machineOwnershipPeriods        MachineOwnershipPeriod[]
  machineIdentifiers             MachineIdentifier[]
  machineMeterReadings           MachineMeterReading[]
  machineMeterReadingCorrections MachineMeterReadingCorrection[]
  machineMeterReadingReferences  MachineMeterReadingReference[]

  @@map("corporations")
}

model Domain {
  id            String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String      @map("corporation_id") @db.Uuid
  host          String      @unique @db.VarChar(253)
  isActive      Boolean     @default(true) @map("is_active")
  createdAt     DateTime    @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt     DateTime    @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation   Corporation @relation(fields: [corporationId], references: [id], onDelete: Restrict)

  @@index([corporationId])
  @@map("domains")
}

model Company {
  id                             String                               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId                  String                               @map("corporation_id") @db.Uuid
  name                           String                               @db.VarChar(160)
  isActive                       Boolean                              @default(true) @map("is_active")
  createdAt                      DateTime                             @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                      DateTime                             @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation                    Corporation                          @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  sessions                       Session[]
  sensitiveDocumentHarnessRows   SensitiveDocumentProtectionHarness[]
  clients                        Client[]
  fuelSuppliers                  FuelSupplier[]
  employments                    Employment[]
  jobRoles                       JobRole[]
  machineOwnershipPeriods        MachineOwnershipPeriod[]
  machineIdentifiers             MachineIdentifier[]
  machineMeterReadings           MachineMeterReading[]
  machineMeterReadingCorrections MachineMeterReadingCorrection[]
  machineMeterReadingReferences  MachineMeterReadingReference[]

  @@unique([corporationId, id])
  @@unique([corporationId, name])
  @@map("companies")
}

model User {
  id                                    String                               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId                         String                               @map("corporation_id") @db.Uuid
  email                                 String                               @db.VarChar(254)
  passwordHash                          String                               @map("password_hash")
  role                                  UserRole                             @default(MASTER_ADMIN)
  isActive                              Boolean                              @default(true) @map("is_active")
  createdAt                             DateTime                             @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                             DateTime                             @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation                           Corporation                          @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  sessions                              Session[]
  removedClients                        Client[]                             @relation("ClientRemovedBy")
  removedFuelSuppliers                  FuelSupplier[]                       @relation("FuelSupplierRemovedBy")
  machineMeterReadings                  MachineMeterReading[]                @relation("MachineMeterReadingActor")
  machineMeterReadingCorrections        MachineMeterReadingCorrection[]      @relation("MachineMeterReadingCorrectionActor")
  createdProjectDailyReports            ProjectDailyReport[]                 @relation("ProjectDailyReportCreatedBy")
  finalizedProjectDailyReports          ProjectDailyReport[]                 @relation("ProjectDailyReportFinalizedBy")
  createdProjectEmployeeAllocations     ProjectEmployeeAllocation[]          @relation("ProjectEmployeeAllocationCreatedBy")
  endedProjectEmployeeAllocations       ProjectEmployeeAllocation[]          @relation("ProjectEmployeeAllocationEndedBy")
  createdProjectMachineAllocations      ProjectMachineAllocation[]           @relation("ProjectMachineAllocationCreatedBy")
  endedProjectMachineAllocations        ProjectMachineAllocation[]           @relation("ProjectMachineAllocationEndedBy")
  createdProjectMachineShiftAssignments ProjectMachineShiftAssignment[]      @relation("ProjectMachineShiftAssignmentCreatedBy")
  endedProjectMachineShiftAssignments   ProjectMachineShiftAssignment[]      @relation("ProjectMachineShiftAssignmentEndedBy")
  createdWorkFrontEmployeeAssignments   ProjectWorkFrontEmployeeAssignment[] @relation("WorkFrontEmployeeAssignmentCreatedBy")
  endedWorkFrontEmployeeAssignments     ProjectWorkFrontEmployeeAssignment[] @relation("WorkFrontEmployeeAssignmentEndedBy")
  createdWorkFrontMachineAssignments    ProjectWorkFrontMachineAssignment[]  @relation("WorkFrontMachineAssignmentCreatedBy")
  endedWorkFrontMachineAssignments      ProjectWorkFrontMachineAssignment[]  @relation("WorkFrontMachineAssignmentEndedBy")
  endedEmploymentPeriods                EmploymentPeriod[]                   @relation("EmploymentPeriodEndedBy")
  endedEmploymentJobRolePeriods         EmploymentJobRolePeriod[]            @relation("EmploymentJobRolePeriodEndedBy")
  endedProjectTechnicalResponsibilities ProjectTechnicalResponsibility[]     @relation("ProjectTechnicalResponsibilityEndedBy")

  @@unique([corporationId, id])
  @@unique([corporationId, email])
  @@map("users")
}

model Session {
  id                         String                             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId              String                             @map("corporation_id") @db.Uuid
  userId                     String                             @map("user_id") @db.Uuid
  companyId                  String?                            @map("company_id") @db.Uuid
  refreshTokenHash           String?                            @map("refresh_token_hash")
  refreshVersion             Int                                @default(0) @map("refresh_version")
  createdAt                  DateTime                           @default(now()) @map("created_at") @db.Timestamptz(3)
  lastUsedAt                 DateTime                           @default(now()) @map("last_used_at") @db.Timestamptz(3)
  idleExpiresAt              DateTime                           @map("idle_expires_at") @db.Timestamptz(3)
  absoluteExpiresAt          DateTime                           @map("absolute_expires_at") @db.Timestamptz(3)
  revokedAt                  DateTime?                          @map("revoked_at") @db.Timestamptz(3)
  revocationReason           String?                            @map("revocation_reason") @db.VarChar(120)
  corporation                Corporation                        @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  user                       User                               @relation(fields: [corporationId, userId], references: [corporationId, id], onDelete: Restrict)
  company                    Company?                           @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict)
  consumedRefreshCredentials SessionConsumedRefreshCredential[]

  @@index([corporationId, userId])
  @@index([corporationId, companyId])
  @@map("sessions")
}

model SessionConsumedRefreshCredential {
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sessionId      String   @map("session_id") @db.Uuid
  credentialHash String   @unique @map("credential_hash")
  consumedAt     DateTime @map("consumed_at") @db.Timestamptz(3)
  session        Session  @relation(fields: [sessionId], references: [id], onDelete: Cascade)

  @@index([sessionId, consumedAt])
  @@map("session_consumed_refresh_credentials")
}

model SensitiveDocumentProtectionHarness {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId        String                @map("corporation_id") @db.Uuid
  companyId            String                @map("company_id") @db.Uuid
  registryType         String                @map("registry_type") @db.VarChar(64)
  documentType         SensitiveDocumentType @map("document_type")
  ciphertext           String                @db.Text
  iv                   String                @db.VarChar(64)
  authTag              String                @map("auth_tag") @db.VarChar(64)
  encryptionKeyVersion String                @map("encryption_key_version") @db.VarChar(64)
  documentDigest       String                @map("document_digest") @db.VarChar(96)
  isActive             Boolean               @default(true) @map("is_active")
  createdAt            DateTime              @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt            DateTime              @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation          Corporation           @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  company              Company               @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict)

  @@index([corporationId, companyId, registryType])
  @@index([documentDigest])
  @@map("sensitive_document_protection_harness")
}

model Client {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId        String                @map("corporation_id") @db.Uuid
  companyId            String                @map("company_id") @db.Uuid
  entityType           CommercialEntityType  @map("entity_type")
  documentType         SensitiveDocumentType @map("document_type")
  ciphertext           String                @db.Text
  iv                   String                @db.VarChar(64)
  authTag              String                @map("auth_tag") @db.VarChar(64)
  encryptionKeyVersion String                @map("encryption_key_version") @db.VarChar(64)
  documentDigest       String                @map("document_digest") @db.VarChar(96)
  displayName          String                @map("display_name") @db.VarChar(180)
  fullName             String?               @map("full_name") @db.VarChar(180)
  legalName            String?               @map("legal_name") @db.VarChar(180)
  tradeName            String?               @map("trade_name") @db.VarChar(180)
  phone                String?               @db.VarChar(32)
  email                String?               @db.VarChar(254)
  addressLine          String?               @map("address_line") @db.VarChar(220)
  city                 String?               @db.VarChar(120)
  state                String?               @db.VarChar(80)
  postalCode           String?               @map("postal_code") @db.VarChar(24)
  isActive             Boolean               @default(true) @map("is_active")
  inactivatedAt        DateTime?             @map("inactivated_at") @db.Timestamptz(3)
  removedAt            DateTime?             @map("removed_at") @db.Timestamptz(3)
  removedByUserId      String?               @map("removed_by_user_id") @db.Uuid
  createdAt            DateTime              @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt            DateTime              @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation          Corporation           @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  company              Company               @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict)
  removedBy            User?                 @relation("ClientRemovedBy", fields: [corporationId, removedByUserId], references: [corporationId, id], onDelete: Restrict)

  @@unique([corporationId, companyId, id])
  @@index([corporationId, companyId, isActive])
  @@index([corporationId, companyId, createdAt, id])
  @@index([corporationId, companyId, displayName, id])
  @@index([documentDigest])
  @@index([removedAt], map: "clients_removed_at_idx")
  @@map("clients")
}

model FuelSupplier {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId        String                @map("corporation_id") @db.Uuid
  companyId            String                @map("company_id") @db.Uuid
  entityType           CommercialEntityType  @map("entity_type")
  documentType         SensitiveDocumentType @map("document_type")
  ciphertext           String                @db.Text
  iv                   String                @db.VarChar(64)
  authTag              String                @map("auth_tag") @db.VarChar(64)
  encryptionKeyVersion String                @map("encryption_key_version") @db.VarChar(64)
  documentDigest       String                @map("document_digest") @db.VarChar(96)
  displayName          String                @map("display_name") @db.VarChar(180)
  fullName             String?               @map("full_name") @db.VarChar(180)
  legalName            String?               @map("legal_name") @db.VarChar(180)
  tradeName            String?               @map("trade_name") @db.VarChar(180)
  phone                String?               @db.VarChar(32)
  email                String?               @db.VarChar(254)
  addressLine          String?               @map("address_line") @db.VarChar(220)
  addressStreet        String?               @map("address_street") @db.VarChar(160)
  addressNumber        String?               @map("address_number") @db.VarChar(30)
  addressComplement    String?               @map("address_complement") @db.VarChar(100)
  addressNeighborhood  String?               @map("address_neighborhood") @db.VarChar(100)
  city                 String?               @db.VarChar(120)
  state                String?               @db.VarChar(80)
  postalCode           String?               @map("postal_code") @db.VarChar(24)
  isGlobal             Boolean               @default(true) @map("is_global")
  projectId            String?               @map("project_id") @db.Uuid
  isActive             Boolean               @default(true) @map("is_active")
  inactivatedAt        DateTime?             @map("inactivated_at") @db.Timestamptz(3)
  removedAt            DateTime?             @map("removed_at") @db.Timestamptz(3)
  removedByUserId      String?               @map("removed_by_user_id") @db.Uuid
  createdAt            DateTime              @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt            DateTime              @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation          Corporation           @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  company              Company               @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict)
  removedBy            User?                 @relation("FuelSupplierRemovedBy", fields: [corporationId, removedByUserId], references: [corporationId, id], onDelete: Restrict)

  @@unique([corporationId, companyId, id])
  @@index([corporationId, companyId, isActive])
  @@index([corporationId, companyId, createdAt, id])
  @@index([corporationId, companyId, displayName, id])
  @@index([documentDigest])
  @@index([removedAt], map: "fuel_suppliers_removed_at_idx")
  @@map("fuel_suppliers")
}

model FuelType {
  id        String   @id @db.VarChar(32)
  name      String   @db.VarChar(80)
  isActive  Boolean  @default(true) @map("is_active")
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@map("fuel_types")
}

model MeasurementUnit {
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String?  @map("corporation_id") @db.Uuid
  companyId     String?  @map("company_id") @db.Uuid
  code          String   @db.VarChar(24)
  name          String   @db.VarChar(80)
  isActive      Boolean  @default(true) @map("is_active")
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt     DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([corporationId, companyId, code])
  @@index([corporationId, companyId, isActive])
  @@map("measurement_units")
}

model SuppliedItem {
  id                String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId     String   @map("corporation_id") @db.Uuid
  companyId         String   @map("company_id") @db.Uuid
  projectId         String?  @map("project_id") @db.Uuid
  categoryId        String?  @map("category_id") @db.Uuid
  name              String   @db.VarChar(160)
  baseUnitId        String   @map("base_unit_id") @db.Uuid
  valueUnitQuantity Decimal  @default(1.000000) @map("value_unit_quantity") @db.Decimal(18, 6)
  basePrice         Decimal  @default(0.0000) @map("base_price") @db.Decimal(18, 4)
  isGlobal          Boolean  @default(true) @map("is_global")
  isActive          Boolean  @default(true) @map("is_active")
  createdAt         DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt         DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@index([corporationId, companyId, isGlobal, isActive])
  @@index([corporationId, companyId, projectId])
  @@index([corporationId, companyId, categoryId])
  @@map("supplied_items")
}

model SuppliedItemCategory {
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String   @map("corporation_id") @db.Uuid
  companyId     String   @map("company_id") @db.Uuid
  parentId      String?  @map("parent_id") @db.Uuid
  systemKey     String?  @map("system_key") @db.VarChar(32)
  name          String   @db.VarChar(120)
  isActive      Boolean  @default(true) @map("is_active")
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt     DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@index([corporationId, companyId, parentId])
  @@index([corporationId, companyId, isActive])
  @@map("supplied_item_categories")
}

model SupplierOffer {
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId    String   @map("corporation_id") @db.Uuid
  companyId        String   @map("company_id") @db.Uuid
  supplierId       String   @map("supplier_id") @db.Uuid
  itemId           String   @map("item_id") @db.Uuid
  purchaseUnitId   String   @map("purchase_unit_id") @db.Uuid
  conversionToBase Decimal  @map("conversion_to_base") @db.Decimal(18, 6)
  isActive         Boolean  @default(true) @map("is_active")
  createdAt        DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt        DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@index([corporationId, companyId, supplierId])
  @@index([corporationId, companyId, itemId])
  @@map("supplier_offers")
}

model SupplierOfferPrice {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String    @map("corporation_id") @db.Uuid
  companyId     String    @map("company_id") @db.Uuid
  offerId       String    @map("offer_id") @db.Uuid
  price         Decimal   @db.Decimal(18, 4)
  effectiveFrom DateTime  @map("effective_from") @db.Timestamptz(3)
  effectiveTo   DateTime? @map("effective_to") @db.Timestamptz(3)
  createdAt     DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)

  @@index([corporationId, companyId, offerId])
  @@map("supplier_offer_prices")
}

model Person {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId        String                @map("corporation_id") @db.Uuid
  documentType         SensitiveDocumentType @map("document_type")
  ciphertext           String                @db.Text
  iv                   String                @db.VarChar(64)
  authTag              String                @map("auth_tag") @db.VarChar(64)
  encryptionKeyVersion String                @map("encryption_key_version") @db.VarChar(64)
  documentDigest       String                @map("document_digest") @db.VarChar(96)
  displayName          String                @map("display_name") @db.VarChar(180)
  fullName             String                @map("full_name") @db.VarChar(180)
  isActive             Boolean               @default(true) @map("is_active")
  createdAt            DateTime              @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt            DateTime              @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation          Corporation           @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  employments          Employment[]

  @@unique([corporationId, id])
  @@unique([corporationId, documentDigest])
  @@index([corporationId, displayName, id])
  @@index([documentDigest])
  @@map("persons")
}

model Employment {
  id                           String                                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId                String                                      @map("corporation_id") @db.Uuid
  companyId                    String                                      @map("company_id") @db.Uuid
  personId                     String                                      @map("person_id") @db.Uuid
  companyRegistrationNumber    String                                      @map("company_registration_number") @db.VarChar(80)
  state                        EmploymentState                             @default(ACTIVE)
  isActive                     Boolean                                     @default(true) @map("is_active")
  terminatedAt                 DateTime?                                   @map("terminated_at") @db.Timestamptz(3)
  createdAt                    DateTime                                    @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                    DateTime                                    @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation                  Corporation                                 @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  company                      Company                                     @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict)
  person                       Person                                      @relation(fields: [corporationId, personId], references: [corporationId, id], onDelete: Restrict)
  periods                      EmploymentPeriod[]
  jobRolePeriods               EmploymentJobRolePeriod[]
  directWorkFrontAssignments   ProjectWorkFrontEmployeeAssignment[]        @relation("WorkFrontEmployeeAssignmentEmployment")
  operatedWorkFrontAssignments ProjectWorkFrontMachineAssignment[]         @relation("WorkFrontMachineAssignmentOperator")
  supervisedDailyReports       ProjectDailyReport[]                        @relation("ProjectDailyReportSupervisor")
  dailyReportResponsibilities  ProjectDailyReportTechnicalResponsibility[] @relation("ProjectDailyReportTechnicalResponsibilityEmployment")
  dailyReportEntries           ProjectDailyReportEmployee[]                @relation("ProjectDailyReportEmployeeEmployment")
  projectMachineAllocations    ProjectMachineAllocation[]

  @@unique([corporationId, companyId, id])
  @@index([corporationId, companyId, isActive])
  @@index([corporationId, companyId, createdAt, id])
  @@index([corporationId, companyId, companyRegistrationNumber], map: "employments_corporation_id_company_id_company_registration_numb")
  @@index([corporationId, personId])
  @@map("employments")
}

model JobRole {
  id                String                    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId     String                    @map("corporation_id") @db.Uuid
  companyId         String                    @map("company_id") @db.Uuid
  name              String                    @db.VarChar(120)
  normalizedName    String                    @map("normalized_name") @db.VarChar(120)
  isActive          Boolean                   @default(true) @map("is_active")
  createdAt         DateTime                  @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt         DateTime                  @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation       Corporation               @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  company           Company                   @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict)
  employmentPeriods EmploymentJobRolePeriod[]

  @@unique([corporationId, companyId, normalizedName])
  @@index([corporationId, companyId, isActive, name, id])
  @@map("job_roles")
}

model EmploymentJobRolePeriod {
  id            String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String                      @map("corporation_id") @db.Uuid
  companyId     String                      @map("company_id") @db.Uuid
  employmentId  String                      @map("employment_id") @db.Uuid
  jobRoleId     String                      @map("job_role_id") @db.Uuid
  reason        String?                     @db.VarChar(240)
  endedByUserId String?                     @map("ended_by_user_id") @db.Uuid
  endedReason   String?                     @map("ended_reason") @db.VarChar(240)
  effectiveFrom DateTime                    @map("effective_from") @db.Date
  effectiveTo   DateTime?                   @map("effective_to") @db.Date
  createdAt     DateTime                    @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt     DateTime                    @updatedAt @map("updated_at") @db.Timestamptz(3)
  employment    Employment                  @relation(fields: [corporationId, companyId, employmentId], references: [corporationId, companyId, id], onDelete: Restrict)
  jobRole       JobRole                     @relation(fields: [jobRoleId], references: [id], onDelete: Restrict)
  endedBy       User?                       @relation("EmploymentJobRolePeriodEndedBy", fields: [corporationId, endedByUserId], references: [corporationId, id], onDelete: Restrict)
  allocations   ProjectEmployeeAllocation[]

  @@index([corporationId, companyId, employmentId, effectiveFrom, id])
  @@map("employment_job_role_periods")
}

model EmploymentPeriod {
  id                String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId     String     @map("corporation_id") @db.Uuid
  companyId         String     @map("company_id") @db.Uuid
  employmentId      String     @map("employment_id") @db.Uuid
  admissionDate     DateTime   @map("admission_date") @db.Date
  effectiveFrom     DateTime   @map("effective_from") @db.Date
  effectiveTo       DateTime?  @map("effective_to") @db.Date
  terminationReason String?    @map("termination_reason") @db.VarChar(240)
  endedByUserId     String?    @map("ended_by_user_id") @db.Uuid
  createdAt         DateTime   @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt         DateTime   @updatedAt @map("updated_at") @db.Timestamptz(3)
  employment        Employment @relation(fields: [corporationId, companyId, employmentId], references: [corporationId, companyId, id], onDelete: Restrict)
  endedBy           User?      @relation("EmploymentPeriodEndedBy", fields: [corporationId, endedByUserId], references: [corporationId, id], onDelete: Restrict)

  @@index([corporationId, companyId, employmentId])
  @@index([corporationId, companyId, effectiveFrom, id], map: "employment_periods_corporation_id_company_id_effective_from_id_")
  @@map("employment_periods")
}

model Machine {
  id                   String                              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId        String                              @map("corporation_id") @db.Uuid
  name                 String                              @db.VarChar(160)
  description          String?                             @db.VarChar(500)
  type                 MachineType
  manufacturer         String                              @db.VarChar(120)
  model                String                              @db.VarChar(120)
  meterType            MachineMeterType                    @map("meter_type")
  isActive             Boolean                             @default(true) @map("is_active")
  createdAt            DateTime                            @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt            DateTime                            @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation          Corporation                         @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  ownershipPeriods     MachineOwnershipPeriod[]
  identifiers          MachineIdentifier[]
  meterReadings        MachineMeterReading[]
  workFrontAssignments ProjectWorkFrontMachineAssignment[]
  dailyReportEntries   ProjectDailyReportMachine[]

  @@unique([corporationId, id])
  @@index([corporationId, createdAt, id])
  @@index([corporationId, name, id])
  @@map("machines")
}

model MachineOwnershipPeriod {
  id            String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String      @map("corporation_id") @db.Uuid
  companyId     String      @map("company_id") @db.Uuid
  machineId     String      @map("machine_id") @db.Uuid
  effectiveFrom DateTime    @default(now()) @map("effective_from") @db.Timestamptz(3)
  effectiveTo   DateTime?   @map("effective_to") @db.Timestamptz(3)
  createdAt     DateTime    @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt     DateTime    @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation   Corporation @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  company       Company     @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict)
  machine       Machine     @relation(fields: [corporationId, machineId], references: [corporationId, id], onDelete: Restrict)

  @@index([corporationId, companyId, machineId])
  @@index([corporationId, companyId, effectiveFrom, id])
  @@map("machine_ownership_periods")
}

model MachineIdentifier {
  id              String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId   String                @map("corporation_id") @db.Uuid
  companyId       String                @map("company_id") @db.Uuid
  machineId       String                @map("machine_id") @db.Uuid
  kind            MachineIdentifierKind
  value           String                @db.VarChar(80)
  normalizedValue String                @map("normalized_value") @db.VarChar(80)
  releasedAt      DateTime?             @map("released_at") @db.Timestamptz(3)
  createdAt       DateTime              @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt       DateTime              @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation     Corporation           @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  company         Company               @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict)
  machine         Machine               @relation(fields: [corporationId, machineId], references: [corporationId, id], onDelete: Restrict)

  @@index([corporationId, companyId, machineId])
  @@index([corporationId, companyId, kind, normalizedValue])
  @@map("machine_identifiers")
}

model MachineMeterReading {
  id                String                          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId     String                          @map("corporation_id") @db.Uuid
  companyId         String                          @map("company_id") @db.Uuid
  machineId         String                          @map("machine_id") @db.Uuid
  readingSequence   Int                             @map("reading_sequence")
  value             Decimal                         @db.Decimal(14, 2)
  status            MachineMeterReadingStatus       @default(CONFIRMED)
  purpose           MachineMeterReadingPurpose
  actorUserId       String                          @map("actor_user_id") @db.Uuid
  recordedAt        DateTime                        @default(now()) @map("recorded_at") @db.Timestamptz(3)
  createdAt         DateTime                        @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt         DateTime                        @updatedAt @map("updated_at") @db.Timestamptz(3)
  corporation       Corporation                     @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  company           Company                         @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict)
  machine           Machine                         @relation(fields: [corporationId, machineId], references: [corporationId, id], onDelete: Restrict)
  actor             User                            @relation("MachineMeterReadingActor", fields: [corporationId, actorUserId], references: [corporationId, id], onDelete: Restrict)
  corrections       MachineMeterReadingCorrection[]
  references        MachineMeterReadingReference[]
  dailyReportStarts ProjectDailyReportMachine[]     @relation("ProjectDailyReportMachineStartReading")
  dailyReportEnds   ProjectDailyReportMachine[]     @relation("ProjectDailyReportMachineEndReading")

  @@unique([corporationId, companyId, machineId, id])
  @@unique([corporationId, machineId, readingSequence], map: "machine_meter_readings_corporation_id_machine_id_sequence_key")
  @@index([corporationId, companyId, machineId, readingSequence])
  @@map("machine_meter_readings")
}

model MachineMeterReadingCorrection {
  id            String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String              @map("corporation_id") @db.Uuid
  companyId     String              @map("company_id") @db.Uuid
  machineId     String              @map("machine_id") @db.Uuid
  readingId     String              @map("reading_id") @db.Uuid
  actorUserId   String              @map("actor_user_id") @db.Uuid
  oldValue      Decimal             @map("old_value") @db.Decimal(14, 2)
  newValue      Decimal             @map("new_value") @db.Decimal(14, 2)
  reason        String              @db.VarChar(500)
  correctedAt   DateTime            @default(now()) @map("corrected_at") @db.Timestamptz(3)
  createdAt     DateTime            @default(now()) @map("created_at") @db.Timestamptz(3)
  corporation   Corporation         @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  company       Company             @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict, map: "mmr_corrections_company_fkey")
  reading       MachineMeterReading @relation(fields: [corporationId, companyId, machineId, readingId], references: [corporationId, companyId, machineId, id], onDelete: Restrict, map: "mmr_corrections_reading_fkey")
  actor         User                @relation("MachineMeterReadingCorrectionActor", fields: [corporationId, actorUserId], references: [corporationId, id], onDelete: Restrict, map: "machine_meter_reading_corrections_corporation_id_actor_user_id_")

  @@index([corporationId, companyId, machineId, readingId])
  @@map("machine_meter_reading_corrections")
}

model MachineMeterReadingReference {
  id            String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String              @map("corporation_id") @db.Uuid
  companyId     String              @map("company_id") @db.Uuid
  machineId     String              @map("machine_id") @db.Uuid
  readingId     String              @map("reading_id") @db.Uuid
  sourceType    String              @map("source_type") @db.VarChar(80)
  sourceId      String              @map("source_id") @db.VarChar(120)
  createdAt     DateTime            @default(now()) @map("created_at") @db.Timestamptz(3)
  corporation   Corporation         @relation(fields: [corporationId], references: [id], onDelete: Restrict)
  company       Company             @relation(fields: [corporationId, companyId], references: [corporationId, id], onDelete: Restrict, map: "mmr_references_company_fkey")
  reading       MachineMeterReading @relation(fields: [corporationId, companyId, machineId, readingId], references: [corporationId, companyId, machineId, id], onDelete: Restrict, map: "mmr_references_reading_fkey")

  @@unique([corporationId, companyId, readingId, sourceType, sourceId])
  @@index([corporationId, companyId, machineId, readingId])
  @@map("machine_meter_reading_references")
}

model Project {
  id                           String                               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId                String                               @map("corporation_id") @db.Uuid
  companyId                    String                               @map("company_id") @db.Uuid
  name                         String                               @db.VarChar(160)
  address                      String                               @db.VarChar(500)
  addressPostalCode            String?                              @map("address_postal_code") @db.VarChar(8)
  addressStreet                String?                              @map("address_street") @db.VarChar(160)
  addressNumber                String?                              @map("address_number") @db.VarChar(30)
  addressComplement            String?                              @map("address_complement") @db.VarChar(100)
  addressNeighborhood          String?                              @map("address_neighborhood") @db.VarChar(100)
  addressCity                  String?                              @map("address_city") @db.VarChar(100)
  addressState                 String?                              @map("address_state") @db.Char(2)
  latitude                     Decimal?                             @db.Decimal(9, 6)
  longitude                    Decimal?                             @db.Decimal(9, 6)
  contractNumber               String?                              @map("contract_number") @db.VarChar(120)
  normalizedContractNumber     String?                              @map("normalized_contract_number") @db.VarChar(120)
  status                       ProjectLifecycleStatus               @default(PLANNED)
  actualStartedAt              DateTime?                            @map("actual_started_at") @db.Timestamptz(3)
  createdAt                    DateTime                             @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                    DateTime                             @updatedAt @map("updated_at") @db.Timestamptz(3)
  workFronts                   ProjectWorkFront[]
  workFrontEmployeeAssignments ProjectWorkFrontEmployeeAssignment[]
  workFrontMachineAssignments  ProjectWorkFrontMachineAssignment[]
  dailyReports                 ProjectDailyReport[]

  @@unique([corporationId, companyId, id])
  @@index([corporationId, companyId, createdAt, id])
  @@index([corporationId, companyId, name, id])
  @@index([corporationId, companyId, normalizedContractNumber])
  @@map("projects")
}

model ProjectBaseline {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId    String    @map("corporation_id") @db.Uuid
  companyId        String    @map("company_id") @db.Uuid
  projectId        String    @map("project_id") @db.Uuid
  approvedBudget   Decimal   @map("approved_budget") @db.Decimal(18, 2)
  plannedStartDate DateTime  @map("planned_start_date") @db.Date
  plannedEndDate   DateTime? @map("planned_end_date") @db.Date
  effectiveFrom    DateTime  @map("effective_from") @db.Timestamptz(3)
  effectiveTo      DateTime? @map("effective_to") @db.Timestamptz(3)
  createdAt        DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)

  @@index([corporationId, companyId, projectId])
  @@map("project_baselines")
}

model ProjectClientPeriod {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String    @map("corporation_id") @db.Uuid
  companyId     String    @map("company_id") @db.Uuid
  projectId     String    @map("project_id") @db.Uuid
  clientId      String    @map("client_id") @db.Uuid
  effectiveFrom DateTime  @map("effective_from") @db.Timestamptz(3)
  effectiveTo   DateTime? @map("effective_to") @db.Timestamptz(3)

  @@index([corporationId, companyId, projectId])
  @@map("project_client_periods")
}

model ProjectManagerTenure {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String    @map("corporation_id") @db.Uuid
  companyId     String    @map("company_id") @db.Uuid
  projectId     String    @map("project_id") @db.Uuid
  employmentId  String    @map("employment_id") @db.Uuid
  effectiveFrom DateTime  @map("effective_from") @db.Timestamptz(3)
  effectiveTo   DateTime? @map("effective_to") @db.Timestamptz(3)

  @@index([corporationId, companyId, projectId])
  @@map("project_manager_tenures")
}

model ProjectTechnicalResponsibility {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String    @map("corporation_id") @db.Uuid
  companyId     String    @map("company_id") @db.Uuid
  projectId     String    @map("project_id") @db.Uuid
  employmentId  String    @map("employment_id") @db.Uuid
  effectiveFrom DateTime  @map("effective_from") @db.Timestamptz(3)
  effectiveTo   DateTime? @map("effective_to") @db.Timestamptz(3)
  endedByUserId String?   @map("ended_by_user_id") @db.Uuid
  endedReason   String?   @map("ended_reason") @db.VarChar(240)
  endedBy       User?     @relation("ProjectTechnicalResponsibilityEndedBy", fields: [corporationId, endedByUserId], references: [corporationId, id], onDelete: Restrict)

  @@index([corporationId, companyId, projectId])
  @@map("project_technical_responsibilities")
}

model ProjectScheduleRevision {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String    @map("corporation_id") @db.Uuid
  companyId     String    @map("company_id") @db.Uuid
  projectId     String    @map("project_id") @db.Uuid
  effectiveFrom DateTime  @map("effective_from") @db.Timestamptz(3)
  effectiveTo   DateTime? @map("effective_to") @db.Timestamptz(3)

  @@index([corporationId, companyId, projectId])
  @@map("project_schedule_revisions")
}

model ProjectScheduleDay {
  id                 String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId      String                  @map("corporation_id") @db.Uuid
  companyId          String                  @map("company_id") @db.Uuid
  scheduleRevisionId String                  @map("schedule_revision_id") @db.Uuid
  shift              ProjectDailyReportShift @default(DAY)
  dayOfWeek          Int                     @map("day_of_week")
  isWorking          Boolean                 @map("is_working")
  startTime          String?                 @map("start_time") @db.VarChar(5)
  endTime            String?                 @map("end_time") @db.VarChar(5)
  endDayOffset       Int                     @default(0) @map("end_day_offset")

  @@unique([scheduleRevisionId, shift, dayOfWeek])
  @@map("project_schedule_days")
}

model ProjectBreakTemplate {
  id                 String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId      String                  @map("corporation_id") @db.Uuid
  companyId          String                  @map("company_id") @db.Uuid
  scheduleRevisionId String                  @map("schedule_revision_id") @db.Uuid
  shift              ProjectDailyReportShift @default(DAY)
  position           Int
  name               String                  @db.VarChar(120)
  durationMinutes    Int                     @map("duration_minutes")

  @@unique([scheduleRevisionId, shift, position])
  @@map("project_break_templates")
}

model ProjectEmployeeAllocation {
  id                           String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId                String                   @map("corporation_id") @db.Uuid
  companyId                    String                   @map("company_id") @db.Uuid
  projectId                    String                   @map("project_id") @db.Uuid
  employmentId                 String                   @map("employment_id") @db.Uuid
  personId                     String                   @map("person_id") @db.Uuid
  shift                        ProjectDailyReportShift  @default(DAY)
  jobRole                      String                   @map("job_role") @db.VarChar(120)
  employmentJobRolePeriodId    String?                  @map("employment_job_role_period_id") @db.Uuid
  expectedDailyWorkloadMinutes Int                      @map("expected_daily_workload_minutes")
  compensationMode             String                   @map("compensation_mode") @db.VarChar(16)
  compensationValue            Decimal                  @map("compensation_value") @db.Decimal(18, 2)
  overtimeRate                 Decimal                  @map("overtime_rate") @db.Decimal(18, 2)
  effectiveFrom                DateTime                 @map("effective_from") @db.Timestamptz(3)
  effectiveTo                  DateTime?                @map("effective_to") @db.Timestamptz(3)
  createdByUserId              String?                  @map("created_by_user_id") @db.Uuid
  endedByUserId                String?                  @map("ended_by_user_id") @db.Uuid
  endedReason                  String?                  @map("ended_reason") @db.VarChar(500)
  employmentJobRolePeriod      EmploymentJobRolePeriod? @relation(fields: [employmentJobRolePeriodId], references: [id], onDelete: Restrict)
  createdBy                    User?                    @relation("ProjectEmployeeAllocationCreatedBy", fields: [corporationId, createdByUserId], references: [corporationId, id], onDelete: Restrict)
  endedBy                      User?                    @relation("ProjectEmployeeAllocationEndedBy", fields: [corporationId, endedByUserId], references: [corporationId, id], onDelete: Restrict)

  @@index([corporationId, companyId, projectId])
  @@index([employmentJobRolePeriodId])
  @@map("project_employee_allocations")
}

model ProjectMachineAllocation {
  id                   String                          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId        String                          @map("corporation_id") @db.Uuid
  companyId            String                          @map("company_id") @db.Uuid
  projectId            String                          @map("project_id") @db.Uuid
  machineId            String                          @map("machine_id") @db.Uuid
  startMeterReadingId  String                          @map("start_meter_reading_id") @db.Uuid
  operatorEmploymentId String                          @map("operator_employment_id") @db.Uuid
  effectiveFrom        DateTime                        @map("effective_from") @db.Timestamptz(3)
  effectiveTo          DateTime?                       @map("effective_to") @db.Timestamptz(3)
  createdByUserId      String?                         @map("created_by_user_id") @db.Uuid
  endedByUserId        String?                         @map("ended_by_user_id") @db.Uuid
  endedReason          String?                         @map("ended_reason") @db.VarChar(500)
  createdBy            User?                           @relation("ProjectMachineAllocationCreatedBy", fields: [corporationId, createdByUserId], references: [corporationId, id], onDelete: Restrict, map: "pma_created_by_user_fkey")
  endedBy              User?                           @relation("ProjectMachineAllocationEndedBy", fields: [corporationId, endedByUserId], references: [corporationId, id], onDelete: Restrict, map: "pma_ended_by_user_fkey")
  operator             Employment                      @relation(fields: [corporationId, companyId, operatorEmploymentId], references: [corporationId, companyId, id], onDelete: Restrict, map: "project_machine_allocations_operator_employment_fkey")
  shiftAssignments     ProjectMachineShiftAssignment[]

  @@index([corporationId, companyId, projectId])
  @@index([corporationId, companyId, projectId, operatorEmploymentId], map: "project_machine_allocations_operator_employment_idx")
  @@map("project_machine_allocations")
}

model ProjectMachineShiftAssignment {
  id                         String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId              String                   @map("corporation_id") @db.Uuid
  companyId                  String                   @map("company_id") @db.Uuid
  projectId                  String                   @map("project_id") @db.Uuid
  projectMachineAllocationId String                   @map("project_machine_allocation_id") @db.Uuid
  machineId                  String                   @map("machine_id") @db.Uuid
  shift                      ProjectDailyReportShift
  operatorEmploymentId       String                   @map("operator_employment_id") @db.Uuid
  effectiveFrom              DateTime                 @map("effective_from") @db.Timestamptz(3)
  effectiveTo                DateTime?                @map("effective_to") @db.Timestamptz(3)
  createdByUserId            String?                  @map("created_by_user_id") @db.Uuid
  endedByUserId              String?                  @map("ended_by_user_id") @db.Uuid
  endedReason                String?                  @map("ended_reason") @db.VarChar(500)
  projectMachineAllocation   ProjectMachineAllocation @relation(fields: [projectMachineAllocationId], references: [id], onDelete: Cascade)
  createdBy                  User?                    @relation("ProjectMachineShiftAssignmentCreatedBy", fields: [corporationId, createdByUserId], references: [corporationId, id], onDelete: Restrict)
  endedBy                    User?                    @relation("ProjectMachineShiftAssignmentEndedBy", fields: [corporationId, endedByUserId], references: [corporationId, id], onDelete: Restrict)

  @@index([corporationId, companyId, projectId, machineId, shift], map: "pmsa_machine_shift_idx")
  @@index([corporationId, companyId, projectId, operatorEmploymentId], map: "pmsa_operator_history_idx")
  @@index([projectMachineAllocationId], map: "pmsa_allocation_idx")
  @@map("project_machine_shift_assignments")
}

model ProjectFuelAgreement {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId  String    @map("corporation_id") @db.Uuid
  companyId      String    @map("company_id") @db.Uuid
  projectId      String    @map("project_id") @db.Uuid
  fuelSupplierId String    @map("fuel_supplier_id") @db.Uuid
  effectiveFrom  DateTime  @map("effective_from") @db.Timestamptz(3)
  effectiveTo    DateTime? @map("effective_to") @db.Timestamptz(3)

  @@index([corporationId, companyId, projectId])
  @@map("project_fuel_agreements")
}

model ProjectFuelPrice {
  id            String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String    @map("corporation_id") @db.Uuid
  companyId     String    @map("company_id") @db.Uuid
  agreementId   String    @map("agreement_id") @db.Uuid
  fuelTypeId    String    @map("fuel_type_id") @db.VarChar(32)
  pricePerLiter Decimal   @map("price_per_liter") @db.Decimal(18, 4)
  effectiveFrom DateTime  @map("effective_from") @db.Timestamptz(3)
  effectiveTo   DateTime? @map("effective_to") @db.Timestamptz(3)

  @@index([corporationId, companyId, agreementId])
  @@map("project_fuel_prices")
}

model ProjectSupplierOffer {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId    String    @map("corporation_id") @db.Uuid
  companyId        String    @map("company_id") @db.Uuid
  projectId        String    @map("project_id") @db.Uuid
  usageKind        String    @default("material") @map("usage_kind") @db.VarChar(16)
  supplierId       String    @map("supplier_id") @db.Uuid
  itemId           String    @map("item_id") @db.Uuid
  sourceOfferId    String?   @map("source_offer_id") @db.Uuid
  purchaseUnitId   String    @map("purchase_unit_id") @db.Uuid
  conversionToBase Decimal   @map("conversion_to_base") @db.Decimal(18, 6)
  price            Decimal   @db.Decimal(18, 4)
  effectiveFrom    DateTime  @map("effective_from") @db.Timestamptz(3)
  effectiveTo      DateTime? @map("effective_to") @db.Timestamptz(3)
  createdAt        DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)

  @@index([corporationId, companyId, projectId])
  @@index([corporationId, companyId, supplierId])
  @@map("project_supplier_offers")
}

model ProjectSupplierOfferPrice {
  id             String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId  String    @map("corporation_id") @db.Uuid
  companyId      String    @map("company_id") @db.Uuid
  projectOfferId String    @map("project_offer_id") @db.Uuid
  price          Decimal   @db.Decimal(18, 4)
  effectiveFrom  DateTime  @map("effective_from") @db.Timestamptz(3)
  effectiveTo    DateTime? @map("effective_to") @db.Timestamptz(3)
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(3)

  @@index([corporationId, companyId, projectOfferId])
  @@map("project_supplier_offer_prices")
}

model ProjectProductionMetricTarget {
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String   @map("corporation_id") @db.Uuid
  companyId     String   @map("company_id") @db.Uuid
  projectId     String   @map("project_id") @db.Uuid
  metricCode    String   @map("metric_code") @db.VarChar(32)
  targetTotal   Decimal  @map("target_total") @db.Decimal(18, 2)
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt     DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([corporationId, companyId, projectId, metricCode])
  @@index([corporationId, companyId, projectId])
  @@map("project_production_metric_targets")
}

model ProjectQuantityBaselineRevision {
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId   String   @map("corporation_id") @db.Uuid
  companyId       String   @map("company_id") @db.Uuid
  projectId       String   @map("project_id") @db.Uuid
  revision        Int
  reason          String?  @db.VarChar(240)
  createdByUserId String   @map("created_by_user_id") @db.Uuid
  createdAt       DateTime @default(now()) @map("created_at") @db.Timestamptz(3)

  @@unique([corporationId, companyId, projectId, revision])
  @@index([corporationId, companyId, projectId, createdAt])
  @@map("project_quantity_baseline_revisions")
}

model ProjectQuantityBaselineItem {
  id          String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  revisionId  String  @map("revision_id") @db.Uuid
  serviceCode String  @map("service_code") @db.VarChar(48)
  unitCode    String  @map("unit_code") @db.VarChar(32)
  total       Decimal @db.Decimal(18, 2)

  @@unique([revisionId, serviceCode])
  @@index([revisionId])
  @@map("project_quantity_baseline_items")
}

model ProjectWorkFront {
  id                  String                               @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId       String                               @map("corporation_id") @db.Uuid
  companyId           String                               @map("company_id") @db.Uuid
  projectId           String                               @map("project_id") @db.Uuid
  name                String                               @db.VarChar(160)
  location            String?                              @db.VarChar(240)
  notes               String?                              @db.VarChar(1000)
  plannedStartDate    DateTime?                            @map("planned_start_date") @db.Date
  plannedEndDate      DateTime?                            @map("planned_end_date") @db.Date
  requiresEmployees   Boolean                              @default(true) @map("requires_employees")
  requiresMachines    Boolean                              @default(true) @map("requires_machines")
  status              ProjectWorkFrontStatus               @default(PLANNED)
  actualStartedAt     DateTime?                            @map("actual_started_at") @db.Timestamptz(3)
  createdAt           DateTime                             @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt           DateTime                             @updatedAt @map("updated_at") @db.Timestamptz(3)
  project             Project                              @relation(fields: [corporationId, companyId, projectId], references: [corporationId, companyId, id], onDelete: Restrict, map: "pwf_project_fkey")
  employeeAssignments ProjectWorkFrontEmployeeAssignment[]
  machineAssignments  ProjectWorkFrontMachineAssignment[]

  @@unique([corporationId, companyId, projectId, id], map: "pwf_scope_id_uq")
  @@unique([corporationId, companyId, projectId, name])
  @@index([corporationId, companyId, projectId, status])
  @@map("project_work_fronts")
}

model ProjectWorkFrontEmployeeAssignment {
  id              String                                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId   String                                   @map("corporation_id") @db.Uuid
  companyId       String                                   @map("company_id") @db.Uuid
  projectId       String                                   @map("project_id") @db.Uuid
  workFrontId     String                                   @map("work_front_id") @db.Uuid
  employmentId    String                                   @map("employment_id") @db.Uuid
  shift           ProjectDailyReportShift                  @default(DAY)
  source          ProjectWorkFrontEmployeeAssignmentSource
  effectiveFrom   DateTime                                 @map("effective_from") @db.Timestamptz(3)
  effectiveTo     DateTime?                                @map("effective_to") @db.Timestamptz(3)
  createdByUserId String                                   @map("created_by_user_id") @db.Uuid
  endedByUserId   String?                                  @map("ended_by_user_id") @db.Uuid
  endedReason     String?                                  @map("ended_reason") @db.VarChar(500)
  project         Project                                  @relation(fields: [corporationId, companyId, projectId], references: [corporationId, companyId, id], onDelete: Restrict, map: "pwfea_project_fkey")
  workFront       ProjectWorkFront                         @relation(fields: [corporationId, companyId, projectId, workFrontId], references: [corporationId, companyId, projectId, id], onDelete: Restrict, map: "pwfea_front_fkey")
  employment      Employment                               @relation("WorkFrontEmployeeAssignmentEmployment", fields: [corporationId, companyId, employmentId], references: [corporationId, companyId, id], onDelete: Restrict, map: "pwfea_employment_fkey")
  createdBy       User                                     @relation("WorkFrontEmployeeAssignmentCreatedBy", fields: [corporationId, createdByUserId], references: [corporationId, id], onDelete: Restrict, map: "pwfea_created_by_user_fkey")
  endedBy         User?                                    @relation("WorkFrontEmployeeAssignmentEndedBy", fields: [corporationId, endedByUserId], references: [corporationId, id], onDelete: Restrict, map: "pwfea_ended_by_user_fkey")

  @@index([corporationId, companyId, projectId, workFrontId, effectiveFrom], map: "pwfea_front_history_idx")
  @@index([corporationId, companyId, projectId, employmentId, effectiveFrom], map: "pwfea_employee_history_idx")
  @@map("project_work_front_employee_assignments")
}

model ProjectWorkFrontMachineAssignment {
  id                   String                  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId        String                  @map("corporation_id") @db.Uuid
  companyId            String                  @map("company_id") @db.Uuid
  projectId            String                  @map("project_id") @db.Uuid
  workFrontId          String                  @map("work_front_id") @db.Uuid
  machineId            String                  @map("machine_id") @db.Uuid
  shift                ProjectDailyReportShift @default(DAY)
  operatorEmploymentId String                  @map("operator_employment_id") @db.Uuid
  effectiveFrom        DateTime                @map("effective_from") @db.Timestamptz(3)
  effectiveTo          DateTime?               @map("effective_to") @db.Timestamptz(3)
  createdByUserId      String                  @map("created_by_user_id") @db.Uuid
  endedByUserId        String?                 @map("ended_by_user_id") @db.Uuid
  endedReason          String?                 @map("ended_reason") @db.VarChar(500)
  project              Project                 @relation(fields: [corporationId, companyId, projectId], references: [corporationId, companyId, id], onDelete: Restrict, map: "pwfma_project_fkey")
  workFront            ProjectWorkFront        @relation(fields: [corporationId, companyId, projectId, workFrontId], references: [corporationId, companyId, projectId, id], onDelete: Restrict, map: "pwfma_front_fkey")
  machine              Machine                 @relation(fields: [corporationId, machineId], references: [corporationId, id], onDelete: Restrict, map: "pwfma_machine_fkey")
  operator             Employment              @relation("WorkFrontMachineAssignmentOperator", fields: [corporationId, companyId, operatorEmploymentId], references: [corporationId, companyId, id], onDelete: Restrict, map: "pwfma_operator_fkey")
  createdBy            User                    @relation("WorkFrontMachineAssignmentCreatedBy", fields: [corporationId, createdByUserId], references: [corporationId, id], onDelete: Restrict, map: "pwfma_created_by_user_fkey")
  endedBy              User?                   @relation("WorkFrontMachineAssignmentEndedBy", fields: [corporationId, endedByUserId], references: [corporationId, id], onDelete: Restrict, map: "pwfma_ended_by_user_fkey")

  @@index([corporationId, companyId, projectId, workFrontId, effectiveFrom], map: "pwfma_front_history_idx")
  @@index([corporationId, companyId, projectId, machineId, effectiveFrom], map: "pwfma_machine_history_idx")
  @@map("project_work_front_machine_assignments")
}

model ProjectWorkFrontService {
  id                String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId     String                     @map("corporation_id") @db.Uuid
  companyId         String                     @map("company_id") @db.Uuid
  projectId         String                     @map("project_id") @db.Uuid
  workFrontId       String                     @map("work_front_id") @db.Uuid
  serviceCode       String                     @map("service_code") @db.VarChar(48)
  unitCode          String                     @map("unit_code") @db.VarChar(32)
  quantity          Decimal                    @db.Decimal(18, 2)
  productionProfile ProjectProductionProfile   @default(GENERIC) @map("production_profile")
  dmtPolicy         ProjectProductionDmtPolicy @default(OPTIONAL) @map("dmt_policy")

  @@unique([workFrontId, serviceCode])
  @@index([corporationId, companyId, projectId, workFrontId])
  @@map("project_work_front_services")
}

model ProjectWorkFrontLifecycleEvent {
  id            String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String                 @map("corporation_id") @db.Uuid
  companyId     String                 @map("company_id") @db.Uuid
  projectId     String                 @map("project_id") @db.Uuid
  workFrontId   String                 @map("work_front_id") @db.Uuid
  fromStatus    ProjectWorkFrontStatus @map("from_status")
  toStatus      ProjectWorkFrontStatus @map("to_status")
  occurredAt    DateTime               @map("occurred_at") @db.Timestamptz(3)
  actorUserId   String                 @map("actor_user_id") @db.Uuid
  reason        String?                @db.VarChar(500)

  @@index([corporationId, companyId, projectId, workFrontId, occurredAt])
  @@map("project_work_front_lifecycle_events")
}

model ProjectCompensationPaymentTerm {
  id                 String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId      String   @map("corporation_id") @db.Uuid
  companyId          String   @map("company_id") @db.Uuid
  projectId          String   @map("project_id") @db.Uuid
  compensationMode   String   @map("compensation_mode") @db.VarChar(16)
  daysAfterPeriodEnd Int      @map("days_after_period_end")
  createdAt          DateTime @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt          DateTime @updatedAt @map("updated_at") @db.Timestamptz(3)

  @@unique([corporationId, companyId, projectId, compensationMode])
  @@index([corporationId, companyId, projectId])
  @@map("project_compensation_payment_terms")
}

model ProjectLifecycleEvent {
  id            String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String                 @map("corporation_id") @db.Uuid
  companyId     String                 @map("company_id") @db.Uuid
  projectId     String                 @map("project_id") @db.Uuid
  fromStatus    ProjectLifecycleStatus @map("from_status")
  toStatus      ProjectLifecycleStatus @map("to_status")
  occurredAt    DateTime               @map("occurred_at") @db.Timestamptz(3)
  actorUserId   String                 @map("actor_user_id") @db.Uuid
  reason        String?                @db.VarChar(500)

  @@index([corporationId, companyId, projectId, occurredAt, id])
  @@map("project_lifecycle_events")
}

model ProjectDailyReport {
  id                        String                                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId             String                                      @map("corporation_id") @db.Uuid
  companyId                 String                                      @map("company_id") @db.Uuid
  projectId                 String                                      @map("project_id") @db.Uuid
  reportDate                DateTime                                    @map("report_date") @db.Date
  shift                     ProjectDailyReportShift
  shiftOrder                Int                                         @map("shift_order")
  status                    ProjectDailyReportStatus                    @default(DRAFT)
  projectNameSnapshot       String                                      @map("project_name_snapshot") @db.VarChar(160)
  municipalitySnapshot      String?                                     @map("municipality_snapshot") @db.VarChar(100)
  stateSnapshot             String?                                     @map("state_snapshot") @db.Char(2)
  contractSnapshot          String?                                     @map("contract_snapshot") @db.VarChar(120)
  scheduleScaleSnapshot     String                                      @map("schedule_scale_snapshot") @db.VarChar(120)
  supervisorEmploymentId    String                                      @map("supervisor_employment_id") @db.Uuid
  supervisorNameSnapshot    String                                      @map("supervisor_name_snapshot") @db.VarChar(180)
  activityStartTime         String                                      @map("activity_start_time") @db.VarChar(5)
  activityEndTime           String                                      @map("activity_end_time") @db.VarChar(5)
  activityEndDayOffset      Int                                         @default(0) @map("activity_end_day_offset")
  activityTypes             ProjectDailyReportActivityType[]            @map("activity_types")
  climateConditions         ProjectDailyReportClimateCondition[]        @map("climate_conditions")
  dailyRainfallMm           Decimal                                     @default(0) @map("daily_rainfall_mm") @db.Decimal(10, 2)
  monthlyRainfallMm         Decimal                                     @default(0) @map("monthly_rainfall_mm") @db.Decimal(10, 2)
  executedActivities        String                                      @map("executed_activities") @db.Text
  interferences             String?                                     @db.Text
  createdByUserId           String                                      @map("created_by_user_id") @db.Uuid
  finalizedByUserId         String?                                     @map("finalized_by_user_id") @db.Uuid
  finalizedAt               DateTime?                                   @map("finalized_at") @db.Timestamptz(3)
  createdAt                 DateTime                                    @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                 DateTime                                    @updatedAt @map("updated_at") @db.Timestamptz(3)
  project                   Project                                     @relation(fields: [corporationId, companyId, projectId], references: [corporationId, companyId, id], onDelete: Restrict, map: "pdr_project_fkey")
  supervisor                Employment                                  @relation("ProjectDailyReportSupervisor", fields: [corporationId, companyId, supervisorEmploymentId], references: [corporationId, companyId, id], onDelete: Restrict, map: "pdr_supervisor_fkey")
  createdBy                 User                                        @relation("ProjectDailyReportCreatedBy", fields: [corporationId, createdByUserId], references: [corporationId, id], onDelete: Restrict, map: "pdr_created_by_fkey")
  finalizedBy               User?                                       @relation("ProjectDailyReportFinalizedBy", fields: [corporationId, finalizedByUserId], references: [corporationId, id], onDelete: Restrict, map: "pdr_finalized_by_fkey")
  schedulePeriods           ProjectDailyReportSchedulePeriod[]
  technicalResponsibilities ProjectDailyReportTechnicalResponsibility[]
  employeeEntries           ProjectDailyReportEmployee[]
  machineEntries            ProjectDailyReportMachine[]

  @@unique([corporationId, companyId, projectId, id], map: "pdr_scope_id_uq")
  @@unique([corporationId, companyId, projectId, reportDate, shift], map: "pdr_project_date_shift_uq")
  @@index([corporationId, companyId, projectId, reportDate, id], map: "pdr_project_date_idx")
  @@map("project_daily_reports")
}

model ProjectDailyReportSchedulePeriod {
  id             String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId  String             @map("corporation_id") @db.Uuid
  companyId      String             @map("company_id") @db.Uuid
  projectId      String             @map("project_id") @db.Uuid
  dailyReportId  String             @map("daily_report_id") @db.Uuid
  position       Int
  startTime      String             @map("start_time") @db.VarChar(5)
  endTime        String             @map("end_time") @db.VarChar(5)
  startDayOffset Int                @default(0) @map("start_day_offset")
  endDayOffset   Int                @default(0) @map("end_day_offset")
  dailyReport    ProjectDailyReport @relation(fields: [corporationId, companyId, projectId, dailyReportId], references: [corporationId, companyId, projectId, id], onDelete: Cascade, map: "pdrsp_report_fkey")

  @@unique([dailyReportId, position], map: "pdrsp_report_position_uq")
  @@index([corporationId, companyId, projectId, dailyReportId], map: "project_daily_report_schedule_periods_scope_idx")
  @@map("project_daily_report_schedule_periods")
}

model ProjectDailyReportTechnicalResponsibility {
  id            String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId String             @map("corporation_id") @db.Uuid
  companyId     String             @map("company_id") @db.Uuid
  projectId     String             @map("project_id") @db.Uuid
  dailyReportId String             @map("daily_report_id") @db.Uuid
  employmentId  String             @map("employment_id") @db.Uuid
  nameSnapshot  String             @map("name_snapshot") @db.VarChar(180)
  position      Int
  dailyReport   ProjectDailyReport @relation(fields: [corporationId, companyId, projectId, dailyReportId], references: [corporationId, companyId, projectId, id], onDelete: Cascade, map: "pdrtr_report_fkey")
  employment    Employment         @relation("ProjectDailyReportTechnicalResponsibilityEmployment", fields: [corporationId, companyId, employmentId], references: [corporationId, companyId, id], onDelete: Restrict, map: "pdrtr_employment_fkey")

  @@unique([dailyReportId, employmentId], map: "pdrtr_report_employment_uq")
  @@unique([dailyReportId, position], map: "pdrtr_report_position_uq")
  @@index([corporationId, companyId, projectId, dailyReportId], map: "project_daily_report_technical_responsibilities_scope_idx")
  @@map("project_daily_report_technical_responsibilities")
}

model ProjectDailyReportEmployee {
  id                           String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId                String             @map("corporation_id") @db.Uuid
  companyId                    String             @map("company_id") @db.Uuid
  projectId                    String             @map("project_id") @db.Uuid
  dailyReportId                String             @map("daily_report_id") @db.Uuid
  employmentId                 String             @map("employment_id") @db.Uuid
  employeeNameSnapshot         String             @map("employee_name_snapshot") @db.VarChar(180)
  jobRoleSnapshot              String             @map("job_role_snapshot") @db.VarChar(120)
  expectedDailyWorkloadMinutes Int                @map("expected_daily_workload_minutes")
  completedFullShift           Boolean            @map("completed_full_shift")
  regularWorkedMinutes         Int                @map("regular_worked_minutes")
  overtimeMinutes              Int                @default(0) @map("overtime_minutes")
  dailyReport                  ProjectDailyReport @relation(fields: [corporationId, companyId, projectId, dailyReportId], references: [corporationId, companyId, projectId, id], onDelete: Cascade, map: "pdre_report_fkey")
  employment                   Employment         @relation("ProjectDailyReportEmployeeEmployment", fields: [corporationId, companyId, employmentId], references: [corporationId, companyId, id], onDelete: Restrict, map: "pdre_employment_fkey")

  @@unique([dailyReportId, employmentId], map: "project_daily_report_employees_report_employment_key")
  @@index([corporationId, companyId, projectId, employmentId, dailyReportId], map: "project_daily_report_employees_scope_idx")
  @@map("project_daily_report_employees")
}

model ProjectDailyReportMachine {
  id                      String                 @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId           String                 @map("corporation_id") @db.Uuid
  companyId               String                 @map("company_id") @db.Uuid
  projectId               String                 @map("project_id") @db.Uuid
  dailyReportId           String                 @map("daily_report_id") @db.Uuid
  machineId               String                 @map("machine_id") @db.Uuid
  machineNameSnapshot     String                 @map("machine_name_snapshot") @db.VarChar(160)
  manufacturerSnapshot    String                 @map("manufacturer_snapshot") @db.VarChar(120)
  modelSnapshot           String                 @map("model_snapshot") @db.VarChar(120)
  meterTypeSnapshot       MachineMeterType       @map("meter_type_snapshot")
  identifierKindSnapshot  MachineIdentifierKind? @map("identifier_kind_snapshot")
  identifierValueSnapshot String?                @map("identifier_value_snapshot") @db.VarChar(80)
  startMeterReadingId     String                 @map("start_meter_reading_id") @db.Uuid
  startMeterReadingValue  Decimal                @map("start_meter_reading_value") @db.Decimal(14, 2)
  endMeterReadingId       String?                @map("end_meter_reading_id") @db.Uuid
  endMeterReadingValue    Decimal                @map("end_meter_reading_value") @db.Decimal(14, 2)
  dailyReport             ProjectDailyReport     @relation(fields: [corporationId, companyId, projectId, dailyReportId], references: [corporationId, companyId, projectId, id], onDelete: Cascade, map: "pdrm_report_fkey")
  machine                 Machine                @relation(fields: [corporationId, machineId], references: [corporationId, id], onDelete: Restrict, map: "pdrm_machine_fkey")
  startMeterReading       MachineMeterReading    @relation("ProjectDailyReportMachineStartReading", fields: [corporationId, companyId, machineId, startMeterReadingId], references: [corporationId, companyId, machineId, id], onDelete: Restrict, map: "pdrm_start_reading_fkey")
  endMeterReading         MachineMeterReading?   @relation("ProjectDailyReportMachineEndReading", fields: [corporationId, companyId, machineId, endMeterReadingId], references: [corporationId, companyId, machineId, id], onDelete: Restrict, map: "pdrm_end_reading_fkey")

  @@unique([dailyReportId, machineId], map: "project_daily_report_machines_report_machine_key")
  @@index([corporationId, companyId, projectId, machineId, dailyReportId], map: "project_daily_report_machines_scope_idx")
  @@map("project_daily_report_machines")
}

model ProjectProduction {
  id                        String                            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  corporationId             String                            @map("corporation_id") @db.Uuid
  companyId                 String                            @map("company_id") @db.Uuid
  projectId                 String                            @map("project_id") @db.Uuid
  workFrontId               String                            @map("work_front_id") @db.Uuid
  workFrontServiceId        String                            @map("work_front_service_id") @db.Uuid
  serviceCodeSnapshot       String                            @map("service_code_snapshot") @db.VarChar(48)
  unitCodeSnapshot          String                            @map("unit_code_snapshot") @db.VarChar(32)
  productionProfileSnapshot ProjectProductionProfile          @map("production_profile_snapshot")
  dmtPolicySnapshot         ProjectProductionDmtPolicy        @map("dmt_policy_snapshot")
  productionDate            DateTime                          @map("production_date") @db.Date
  shift                     ProjectDailyReportShift
  shiftOrder                Int                               @map("shift_order")
  status                    ProjectProductionStatus           @default(DRAFT)
  entryMode                 ProjectProductionEntryMode        @map("entry_mode")
  revision                  Int                               @default(1)
  startTime                 String?                           @map("start_time") @db.VarChar(5)
  endTime                   String?                           @map("end_time") @db.VarChar(5)
  endDayOffset              Int                               @default(0) @map("end_day_offset")
  responsibleEmploymentId   String?                           @map("responsible_employment_id") @db.Uuid
  responsibleNameSnapshot   String?                           @map("responsible_name_snapshot") @db.VarChar(180)
  location                  String?                           @db.VarChar(240)
  startStation              String?                           @map("start_station") @db.VarChar(80)
  endStation                String?                           @map("end_station") @db.VarChar(80)
  layer                     String?                           @db.VarChar(80)
  elevation                 String?                           @db.VarChar(80)
  materialName              String?                           @map("material_name") @db.VarChar(160)
  materialCategory          String?                           @map("material_category") @db.VarChar(120)
  volumeCondition           ProjectProductionVolumeCondition? @map("volume_condition")
  directQuantity            Decimal?                          @map("direct_quantity") @db.Decimal(18, 3)
  measuredQuantity          Decimal?                          @map("measured_quantity") @db.Decimal(18, 3)
  conversionFactor          Decimal?                          @map("conversion_factor") @db.Decimal(12, 6)
  origin                    String?                           @db.VarChar(240)
  destination               String?                           @db.VarChar(240)
  dmtKm                     Decimal?                          @map("dmt_km") @db.Decimal(10, 3)
  layerThicknessCm          Decimal?                          @map("layer_thickness_cm") @db.Decimal(10, 2)
  compactionPasses          Int?                              @map("compaction_passes")
  moistureCondition         String?                           @map("moisture_condition") @db.VarChar(120)
  evidence                  Json                              @default("[]")
  notes                     String?                           @db.Text
  createdByUserId           String                            @map("created_by_user_id") @db.Uuid
  approvedByUserId          String?                           @map("approved_by_user_id") @db.Uuid
  approvedAt                DateTime?                         @map("approved_at") @db.Timestamptz(3)
  lastReopenReason          String?                           @map("last_reopen_reason") @db.VarChar(500)
  createdAt                 DateTime                          @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                 DateTime                          @updatedAt @map("updated_at") @db.Timestamptz(3)
  equipment                 ProjectProductionEquipment[]
  trips                     ProjectProductionTrip[]
  revisions                 ProjectProductionRevision[]
  dailyReportLinks          ProjectDailyReportProduction[]

  @@unique([corporationId, companyId, projectId, id], map: "project_productions_scope_id_uq")
  @@index([corporationId, companyId, projectId, productionDate, shiftOrder, id], map: "project_productions_project_date_idx")
  @@index([corporationId, companyId, projectId, workFrontId, status], map: "project_productions_front_status_idx")
  @@index([corporationId, companyId, projectId, serviceCodeSnapshot, status], map: "project_productions_service_status_idx")
  @@map("project_productions")
}

model ProjectProductionEquipment {
  id                    String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  productionId          String                         @map("production_id") @db.Uuid
  machineId             String                         @map("machine_id") @db.Uuid
  machineNameSnapshot   String                         @map("machine_name_snapshot") @db.VarChar(160)
  manufacturerSnapshot  String                         @map("manufacturer_snapshot") @db.VarChar(120)
  modelSnapshot         String                         @map("model_snapshot") @db.VarChar(120)
  identifierSnapshot    String?                        @map("identifier_snapshot") @db.VarChar(80)
  meterTypeSnapshot     MachineMeterType               @map("meter_type_snapshot")
  role                  ProjectProductionEquipmentRole
  operatorEmploymentId  String?                        @map("operator_employment_id") @db.Uuid
  operatorNameSnapshot  String?                        @map("operator_name_snapshot") @db.VarChar(180)
  initialMeterValue     Decimal?                       @map("initial_meter_value") @db.Decimal(14, 2)
  finalMeterValue       Decimal?                       @map("final_meter_value") @db.Decimal(14, 2)
  workedMinutes         Int?                           @map("worked_minutes")
  defaultTripCapacityM3 Decimal?                       @map("default_trip_capacity_m3") @db.Decimal(10, 3)
  createdAt             DateTime                       @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt             DateTime                       @updatedAt @map("updated_at") @db.Timestamptz(3)
  production            ProjectProduction              @relation(fields: [productionId], references: [id], onDelete: Cascade)
  stops                 ProjectProductionStop[]
  trips                 ProjectProductionTrip[]

  @@unique([productionId, machineId], map: "project_production_equipment_machine_uq")
  @@index([machineId])
  @@map("project_production_equipment")
}

model ProjectProductionStop {
  id                    String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  productionEquipmentId String                     @map("production_equipment_id") @db.Uuid
  durationMinutes       Int                        @map("duration_minutes")
  reason                String                     @db.VarChar(160)
  notes                 String?                    @db.VarChar(500)
  createdAt             DateTime                   @default(now()) @map("created_at") @db.Timestamptz(3)
  equipment             ProjectProductionEquipment @relation(fields: [productionEquipmentId], references: [id], onDelete: Cascade)

  @@index([productionEquipmentId])
  @@map("project_production_stops")
}

model ProjectProductionTrip {
  id                    String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  productionId          String                     @map("production_id") @db.Uuid
  productionEquipmentId String                     @map("production_equipment_id") @db.Uuid
  idempotencyKey        String                     @map("idempotency_key") @db.Uuid
  recordedAt            DateTime                   @map("recorded_at") @db.Timestamptz(3)
  capacityM3            Decimal                    @map("capacity_m3") @db.Decimal(10, 3)
  adjustedVolumeM3      Decimal?                   @map("adjusted_volume_m3") @db.Decimal(10, 3)
  ticketNumber          String?                    @map("ticket_number") @db.VarChar(80)
  notes                 String?                    @db.VarChar(500)
  createdByUserId       String                     @map("created_by_user_id") @db.Uuid
  createdAt             DateTime                   @default(now()) @map("created_at") @db.Timestamptz(3)
  production            ProjectProduction          @relation(fields: [productionId], references: [id], onDelete: Cascade)
  equipment             ProjectProductionEquipment @relation(fields: [productionEquipmentId], references: [id], onDelete: Restrict)

  @@unique([productionId, idempotencyKey], map: "project_production_trip_idempotency_uq")
  @@index([productionId, recordedAt, id])
  @@map("project_production_trips")
}

model ProjectProductionRevision {
  id           String                         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  productionId String                         @map("production_id") @db.Uuid
  revision     Int
  event        ProjectProductionRevisionEvent
  reason       String?                        @db.VarChar(500)
  snapshot     Json
  actorUserId  String                         @map("actor_user_id") @db.Uuid
  createdAt    DateTime                       @default(now()) @map("created_at") @db.Timestamptz(3)
  production   ProjectProduction              @relation(fields: [productionId], references: [id], onDelete: Cascade)

  @@unique([productionId, revision], map: "project_production_revision_uq")
  @@index([productionId, createdAt])
  @@map("project_production_revisions")
}

model ProjectDailyReportProduction {
  id                String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dailyReportId     String            @map("daily_report_id") @db.Uuid
  productionId      String            @map("production_id") @db.Uuid
  confirmedRevision Int               @map("confirmed_revision")
  isStale           Boolean           @default(false) @map("is_stale")
  confirmedByUserId String            @map("confirmed_by_user_id") @db.Uuid
  confirmedAt       DateTime          @map("confirmed_at") @db.Timestamptz(3)
  production        ProjectProduction @relation(fields: [productionId], references: [id], onDelete: Restrict)

  @@unique([dailyReportId, productionId], map: "project_daily_report_production_uq")
  @@index([dailyReportId, isStale])
  @@map("project_daily_report_productions")
}

model IdempotencyRecord {
  id            String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sessionId     String   @map("session_id") @db.Uuid
  corporationId String   @map("corporation_id") @db.Uuid
  companyId     String   @map("company_id") @db.Uuid
  operation     String   @db.VarChar(80)
  key           String   @db.Uuid
  requestHash   String   @map("request_hash") @db.VarChar(64)
  projectId     String   @map("project_id") @db.Uuid
  completedAt   DateTime @map("completed_at") @db.Timestamptz(3)
  expiresAt     DateTime @map("expires_at") @db.Timestamptz(3)

  @@unique([sessionId, corporationId, companyId, operation, key])
  @@index([expiresAt])
  @@map("idempotency_records")
}
